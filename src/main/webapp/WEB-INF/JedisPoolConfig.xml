<?xml version="1.0"?>
<JedisPoolConf>
	
	<!-- 视频回放观看人数 -->
	<!-- 192.168.100.17 6389 -->
	<redisSource name="BangVideoSource">
		<connectionHost>redis4.kktv2.com</connectionHost>
	    <connectionPort>6389</connectionPort>
	    <connectionPassword>kkgamevideo</connectionPassword>
	    <connectionDatabase>2</connectionDatabase>
	    <connectionTimeOut>5000</connectionTimeOut>
	    <maxActive>10</maxActive>
	    <maxIdle>5</maxIdle>
	    <minIdle>1</minIdle>
	    <testOnBorrow>false</testOnBorrow>
	    <testOnReturn>false</testOnReturn>
	    <testWhileIdle>true</testWhileIdle>
	    <maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.19 6383 tshowtask 10 -->
    <redisSource name="BangLevel">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6383</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowtask</connectionPassword>
	  	<!-- Redis Server Database -->
	  	<connectionDatabase>10</connectionDatabase>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.37 6380 tshownnhh 6 -->
	<redisSource name="RoomInvition">
		<!-- Redis Server Host -->
		<connectionHost>redis1.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6380</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshownnhh</connectionPassword>
		<!-- Redis Server DB -->
		<connectionDatabase>6</connectionDatabase>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as 
			always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method 
			is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the 
			pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted 
			action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.17 6385 -->
	<redisSource name="SearchWords">
		<!-- Redis Server Host -->
		<connectionHost>redis4.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6385</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshowfw</connectionPassword>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as 
			always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method 
			is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the 
			pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted 
			action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.37 6395 -->
	<redisSource name="Channel">
		<!-- Redis Server Host -->
		<connectionHost>redis1.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6395</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>channel</connectionPassword>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as 
			always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method 
			is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the 
			pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted 
			action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.17 6385 -->
	<redisSource name="News">
		<!-- Redis Server Host -->
		<connectionHost>redis4.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6385</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshowfw</connectionPassword>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Redis Server DB -->
		<connectionDatabase>5</connectionDatabase>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.19 6386 -->
   	<redisSource name="Guess">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6386</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowsms</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
   	
   	<!-- 192.168.100.17 6385 -->
    <redisSource name="FamilyHonorCache">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.19 6386 -->
	<redisSource name="LotteryCache">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6386</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowsms</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
   	 
   	<!-- 192.168.100.10 6379 -->
    <redisSource name="HotData">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis2.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6379</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowpwdd</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.10 6379 -->
    <redisSource name="UserHot">
		<!-- Redis Server Host -->
       	<connectionHost>redis2.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6379</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshowpwdd</connectionPassword>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.17 6382 -->
    <redisSource name="UserRelation">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6382</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.37 6385 -->
    <redisSource name="UserCar">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.19 6383 -->
    <redisSource name="AppStats">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6383</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowtask</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.17 6385 -->
	<redisSource name="UserRecharge">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.37 6385 -->
    <redisSource name="QQVip">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.17 6385 -->
    <redisSource name="Family">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.17 6385 -->
    <redisSource name="FamilyApply">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>5</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>3</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.10 6381 -->
    <redisSource name="GiftRecord">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis2.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6381</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Idle connection checking period -->
	  	<timeBetweenEvictionRunsMillis>60000</timeBetweenEvictionRunsMillis>
    </redisSource>
    
    <!-- 192.168.100.37 6381 -->
    <redisSource name="WeekGift">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6381</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Idle connection checking period -->
	  	<timeBetweenEvictionRunsMillis>60000</timeBetweenEvictionRunsMillis>
    </redisSource>
    
	<!-- 用户消息配置 -->
	<!-- 192.168.100.37 6379 -->
    <redisSource name="UserMessage">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6379</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowmsg</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Idle connection checking period -->
	  	<timeBetweenEvictionRunsMillis>60000</timeBetweenEvictionRunsMillis>
    </redisSource>
    
    <!-- 192.168.100.37 6381 -->
    <redisSource name="Match">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6381</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.19 6386 -->
    <redisSource name="Sms">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6386</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowsms</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
    
    <!-- 192.168.100.37 6383 -->
    <redisSource name="UserProp">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6383</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowgift</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
	  	<maxActive>10</maxActive>
	  	<!-- Number of connections to Redis that just sit there and do nothing -->
	  	<maxIdle>5</maxIdle>
	  	<!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
	  	<minIdle>1</minIdle>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
	  	<maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.19 6392 -->
    <redisSource name="Activity">
        <!-- Redis Server Host -->
        <connectionHost>redis3.kktv2.com</connectionHost>
        <!-- Redis Server Port -->
        <connectionPort>6392</connectionPort>
        <!-- Redis Server Password -->
        <connectionPassword>tshowact</connectionPassword>
        <!-- Redis Server Database -->
        <connectionDatabase>0</connectionDatabase>
        <!-- Redis Server TimeOut -->
        <connectionTimeOut>5000</connectionTimeOut>
        <!-- Maximum active connections to Redis instance -->
        <maxActive>10</maxActive>
        <!-- Number of connections to Redis that just sit there and do nothing -->
        <maxIdle>5</maxIdle>
        <!-- Minimum number of idle connections to Redis - these can be seen as always open and ready to serve -->
        <minIdle>1</minIdle>
        <!-- Tests whether connection is dead when connection retrieval method is called -->
        <testOnBorrow>false</testOnBorrow>
        <!-- Tests whether connection is dead when returning a connection to the pool -->
        <testOnReturn>false</testOnReturn>
        <!-- Tests whether connections are dead during idle periods -->
        <testWhileIdle>true</testWhileIdle>
        <!-- Maximum time, in milliseconds, to wait for a resource when exausted action is set to WHEN_EXAUSTED_BLOCK -->
        <maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.37 6395 channel -->
	<redisSource name="DragonBoat">
		<connectionHost>redis1.kktv2.com</connectionHost>
		<connectionPort>6395</connectionPort>
		<connectionPassword>channel</connectionPassword>
		<connectionTimeOut>5000</connectionTimeOut>
		<connectionDatabase>9</connectionDatabase>
		<maxActive>10</maxActive>
		<maxIdle>5</maxIdle>
		<minIdle>1</minIdle>
		<testOnBorrow>false</testOnBorrow>
		<testOnReturn>false</testOnReturn>
		<testWhileIdle>true</testWhileIdle>
		<maxWait>3000</maxWait>
	</redisSource>

	<!-- 192.168.100.19 6386 -->
    <redisSource name="GameRankingList">
		   <connectionHost>redis3.kktv2.com</connectionHost>
		   <connectionPort>6386</connectionPort>
		   <connectionPassword>tshowsms</connectionPassword>
		   <connectionDatabase>15</connectionDatabase>
		   <connectionTimeOut>5000</connectionTimeOut>
		   <maxActive>10</maxActive>
		   <maxIdle>5</maxIdle>
		   <minIdle>1</minIdle>
		   <testOnBorrow>false</testOnBorrow>
		   <testOnReturn>false</testOnReturn>
		   <testWhileIdle>true</testWhileIdle>
		   <maxWait>3000</maxWait>
    </redisSource>
	
	<!-- 192.168.100.17 6385 tshowfw -->
	<redisSource name="famlilyRoom">
	    <!-- Redis Server Host -->
	  	<connectionHost>redis4.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6385</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowfw</connectionPassword>
	    <!-- Redis Server TimeOut -->
	    <connectionTimeOut>5000</connectionTimeOut>
	    <!-- Maximum active connections to Redis instance -->
	    <maxActive>10</maxActive>
	    <!-- Number of connections to Redis that just sit there and do nothing -->
	    <maxIdle>5</maxIdle>
	    <!-- Minimum number of idle connections to Redis - these can be seen as
	            always open and ready to serve -->
	    <minIdle>1</minIdle>
	    <!-- Tests whether connection is dead when connection retrieval method
	            is called -->
	    <testOnBorrow>false</testOnBorrow>
	    <!-- Tests whether connection is dead when returning a connection to the
	            pool -->
	    <testOnReturn>false</testOnReturn>
	    <!-- Tests whether connections are dead during idle periods -->
	    <testWhileIdle>true</testWhileIdle>
	    <!-- Maximum time, in milliseconds, to wait for a resource when exausted
	            action is set to WHEN_EXAUSTED_BLOCK -->
	    <maxWait>3000</maxWait>
	</redisSource>
	
	<!-- 192.168.100.19 6392 -->
	<redisSource name="KKHallSource">
		<!-- Redis Server Host -->
		<connectionHost>redis3.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6392</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshowact</connectionPassword>
		<!-- Redis Server DB -->
		<connectionDatabase>1</connectionDatabase>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>5</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as 
			always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method 
			is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the 
			pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted 
			action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
	</redisSource>

	<!-- 192.168.100.19 6386 -->
	<redisSource name="ActorInfoSource">
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<connectionPort>6386</connectionPort>
	  	<connectionPassword>tshowsms</connectionPassword>
	  	<connectionDatabase>15</connectionDatabase>
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<maxActive>10</maxActive>
	  	<maxIdle>5</maxIdle>
	  	<minIdle>1</minIdle>
	  	<testOnBorrow>false</testOnBorrow>
	  	<testOnReturn>false</testOnReturn>
	  	<testWhileIdle>true</testWhileIdle>
	  	<maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.19 6383 -->
	<redisSource name="GamblingSource">
		<connectionHost>redis3.kktv2.com</connectionHost>
		<connectionPort>6383</connectionPort>
		<connectionPassword>tshowtask</connectionPassword>
		<connectionDatabase>15</connectionDatabase>
		<connectionTimeOut>5000</connectionTimeOut>
		<testOnBorrow>false</testOnBorrow>
		<testOnReturn>false</testOnReturn>
		<testWhileIdle>true</testWhileIdle>
		<timeBetweenEvictionRunsMillis>60000</timeBetweenEvictionRunsMillis>
	</redisSource>

	<!-- 192.168.100.19 6386 -->
	<redisSource name="RankingList">
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<connectionPort>6386</connectionPort>
	  	<connectionPassword>tshowsms</connectionPassword>
	  	<connectionDatabase>15</connectionDatabase>
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<maxActive>10</maxActive>
	  	<maxIdle>5</maxIdle>
	  	<minIdle>1</minIdle>
	  	<testOnBorrow>false</testOnBorrow>
	  	<testOnReturn>false</testOnReturn>
	  	<testWhileIdle>true</testWhileIdle>
	  	<maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.19 6383 -->
    <redisSource name="GiftStarSource">
		<connectionHost>redis3.kktv2.com</connectionHost>
		<connectionPort>6383</connectionPort>
		<connectionPassword>tshowtask</connectionPassword>
	  	<connectionDatabase>14</connectionDatabase>
		<connectionTimeOut>5000</connectionTimeOut>
		<maxActive>5</maxActive>
		<maxIdle>3</maxIdle>
		<minIdle>1</minIdle>
		<testOnBorrow>false</testOnBorrow>
		<testOnReturn>false</testOnReturn>
		<testWhileIdle>true</testWhileIdle>
		<maxWait>3000</maxWait>
	</redisSource>

	<!-- 192.168.100.19 6383 -->
    <redisSource name="SignInTask">
	  	<connectionHost>redis3.kktv2.com</connectionHost>
	  	<connectionPort>6383</connectionPort>
	  	<connectionPassword>tshowtask</connectionPassword>
	  	<connectionDatabase>15</connectionDatabase>
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<maxActive>10</maxActive>
	  	<maxIdle>5</maxIdle>
	  	<minIdle>1</minIdle>
	  	<testOnBorrow>false</testOnBorrow>
	  	<testOnReturn>false</testOnReturn>
	  	<testWhileIdle>true</testWhileIdle>
	  	<maxWait>3000</maxWait>
    </redisSource>

    <!-- 房间内消息通道 -->
    <!-- "ip" : "192.168.100.24","port" : 6379,"password" : "channel" -->
    <redisSource name="RoomMessageSource">
	    <connectionHost>nodeapi.kktv2.com</connectionHost>
	    <connectionPort>6379</connectionPort>
	    <connectionPassword>local</connectionPassword>
	    <connectionTimeOut>5000</connectionTimeOut>
	    <maxActive>10</maxActive>
	    <maxIdle>5</maxIdle>
	    <minIdle>1</minIdle>
	    <testOnBorrow>false</testOnBorrow>
	    <testOnReturn>false</testOnReturn>
	    <testWhileIdle>true</testWhileIdle>
	    <maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.37 6398 -->
	<redisSource name="RoomConfig">
	    <connectionHost>redis1.kktv2.com</connectionHost>
	    <connectionPort>6398</connectionPort>
	    <connectionPassword>tshowroom</connectionPassword>
	    <connectionTimeOut>5000</connectionTimeOut>
	    <maxActive>10</maxActive>
	    <maxIdle>5</maxIdle>
	    <minIdle>1</minIdle>
	    <testOnBorrow>false</testOnBorrow>
	    <testOnReturn>false</testOnReturn>
	    <testWhileIdle>true</testWhileIdle>
	    <maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.10 6381 -->
    <redisSource name="GiftInfo">
	    <connectionHost>redis2.kktv2.com</connectionHost>
	    <connectionPort>6381</connectionPort>
	    <connectionPassword>tshowgift</connectionPassword>
	    <connectionTimeOut>5000</connectionTimeOut>
	    <maxActive>10</maxActive>
	    <maxIdle>5</maxIdle>
	    <minIdle>1</minIdle>
	    <testOnBorrow>false</testOnBorrow>
	    <testOnReturn>false</testOnReturn>
	    <testWhileIdle>true</testWhileIdle>
	    <maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.37 6398 -->
    <redisSource name="Messaga">
	    <connectionHost>redis1.kktv2.com</connectionHost>
	    <connectionPort>6398</connectionPort>
	    <connectionPassword>tshowroom</connectionPassword>
	    <connectionTimeOut>5000</connectionTimeOut>
	    <maxActive>10</maxActive>
	    <maxIdle>5</maxIdle>
	    <minIdle>1</minIdle>
	    <testOnBorrow>false</testOnBorrow>
	    <testOnReturn>false</testOnReturn>
	    <testWhileIdle>true</testWhileIdle>
	    <maxWait>3000</maxWait>
    </redisSource>

	<!-- 192.168.100.37 6380 -->
	<redisSource name="NewsV2">
		<!-- Redis Server Host -->
		<connectionHost>redis1.kktv2.com</connectionHost>
		<!-- Redis Server Port -->
		<connectionPort>6380</connectionPort>
		<!-- Redis Server Password -->
		<connectionPassword>tshownnhh</connectionPassword>
		<!-- Redis Server TimeOut -->
		<connectionTimeOut>5000</connectionTimeOut>
		<!-- Redis Server DB -->
		<connectionDatabase>3</connectionDatabase>
		<!-- Maximum active connections to Redis instance -->
		<maxActive>10</maxActive>
		<!-- Number of connections to Redis that just sit there and do nothing -->
		<maxIdle>3</maxIdle>
		<!-- Minimum number of idle connections to Redis - these can be seen as 
			always open and ready to serve -->
		<minIdle>1</minIdle>
		<!-- Tests whether connection is dead when connection retrieval method 
			is called -->
		<testOnBorrow>false</testOnBorrow>
		<!-- Tests whether connection is dead when returning a connection to the 
			pool -->
		<testOnReturn>false</testOnReturn>
		<!-- Tests whether connections are dead during idle periods -->
		<testWhileIdle>true</testWhileIdle>
		<!-- Maximum time, in milliseconds, to wait for a resource when exausted 
			action is set to WHEN_EXAUSTED_BLOCK -->
		<maxWait>3000</maxWait>
    </redisSource>

	<!-- 手机端消息推送 -->
	<!-- 192.168.100.37 6379 -->
    <redisSource name="push_source_list">
    	<!-- Redis Server Host -->
	  	<connectionHost>redis1.kktv2.com</connectionHost>
	  	<!-- Redis Server Port -->
	  	<connectionPort>6379</connectionPort>
	  	<!-- Redis Server Password -->
	  	<connectionPassword>tshowmsg</connectionPassword>
	  	<!-- Redis Server TimeOut -->
	  	<connectionTimeOut>5000</connectionTimeOut>
	  	<!-- Tests whether connection is dead when connection retrieval method is called -->
	  	<testOnBorrow>false</testOnBorrow>
	  	<!-- Tests whether connection is dead when returning a connection to the pool -->
	  	<testOnReturn>false</testOnReturn>
	  	<!-- Tests whether connections are dead during idle periods -->
	  	<testWhileIdle>true</testWhileIdle>
	  	<!-- Idle connection checking period -->
	  	<timeBetweenEvictionRunsMillis>60000</timeBetweenEvictionRunsMillis>
    </redisSource>

</JedisPoolConf>
